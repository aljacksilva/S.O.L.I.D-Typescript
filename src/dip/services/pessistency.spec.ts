describe('Testing of something', () => {
  it('descrition of test (IT)', () => {
    const number = 1;
    expect(number).toBe(1);
  });
});

describe('Testing of something', () => {
  test('descrition of test (TEST)', () => {
    const name = 'Jack';
    expect(name).toBe('Jack');
  });
});
